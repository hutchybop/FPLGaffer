# If not already create a python enviroment
    # python3 -m venv <env name>
    # source <env name>/bin/activate
# Before this Run: 
    # pip3 install --upgrade pip setuptools wheel
# Install with: 
    # pip3 install -r requirements.txt

setuptools
wheel
requests
openai
python-dotenv
tabulate
pandas
numpy
scikit-learn
flask
gunicorn
pulp
